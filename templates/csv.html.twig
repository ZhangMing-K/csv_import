<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Your CSV submitted</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                background-color: #f5f5f5;
            }
            h1 {
                color: #4CAF50;
            }
            form {
                font-family: Arial, sans-serif;
                background-color: #f5f5f5;
                padding: 10px;
                border-radius: 5px;
            }
            input[type="file"] {
                border: none;
                background-color: white;
                padding: 10px;
                border-radius: 5px;
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            }
            button[type="submit"] {
                background-color: #4CAF50;
                color: white;
                border: none;
                padding: 10px 20px;
                border-radius: 5px;
                cursor: pointer;
            }
        </style>
    </head>
    <body>
        {{ form_start(form) }}
            {{ form_widget(form.csv_file) }}
            {{ form_errors(form.csv_file) }}
            {{ form_rest(form) }}
            <button type="submit">Import URLs</button>
        {{ form_end(form) }}
    </body>
</html>



